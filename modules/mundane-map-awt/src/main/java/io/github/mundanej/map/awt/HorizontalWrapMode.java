package io.github.mundanej.map.awt;

/** Explicit per-binding horizontal display-repetition mode. */
public enum HorizontalWrapMode {
    /** Preserve ordinary non-repeating source and snapshot behavior. */
    NONE,

    /** Repeat canonical content across the view's configured horizontal period. */
    REPEAT_X
}
