/**
 * Closed-world smoke scenarios compiled into the project's GraalVM Native Image executable.
 *
 * <p>This support package is verification infrastructure rather than a published runtime API. The
 * supported host, toolchain, linkage, capability, and exclusion profile is maintained in the
 * module's {@code CAPABILITIES.md}.
 */
package io.github.mundanej.map.nativeimage;
