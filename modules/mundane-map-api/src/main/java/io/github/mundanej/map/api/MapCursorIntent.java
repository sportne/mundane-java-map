package io.github.mundanej.map.api;

/** Toolkit-neutral cursor requested by an active map tool. */
public enum MapCursorIntent {
    /** Host default cursor. */
    DEFAULT,
    /** Crosshair cursor. */
    CROSSHAIR,
    /** Pointing-hand cursor. */
    HAND,
    /** Move cursor. */
    MOVE
}
