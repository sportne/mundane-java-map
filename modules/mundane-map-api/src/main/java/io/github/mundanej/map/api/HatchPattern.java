package io.github.mundanej.map.api;

/** The bounded Level 1 diagonal hatch patterns. */
public enum HatchPattern {
    /** Lines rising toward screen right. */
    FORWARD_DIAGONAL,
    /** Lines falling toward screen right. */
    BACKWARD_DIAGONAL,
    /** Forward and backward diagonals in that paint order. */
    CROSS_DIAGONAL
}
