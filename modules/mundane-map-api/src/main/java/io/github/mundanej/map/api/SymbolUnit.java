package io.github.mundanej.map.api;

/** The unit used by a symbol measurement. */
public enum SymbolUnit {
    /** Logical Swing component pixels before device scaling. */
    SCREEN_PIXEL,
    /** Projected-world units in the active map viewport. */
    MAP_UNIT
}
