package io.github.mundanej.map.api;

/** Uses the feature's immutable display name as point-label text. */
public enum FeatureName implements LabelTextSource {
    /** The sole feature-name text source. */
    INSTANCE
}
