/**
 * Vaadin Flow Canvas bindings for immutable snapshots, serialized common feature-source queries,
 * bounded built-in vector symbols, browser-measured point labels, toolkit-neutral interaction,
 * measurement, fixed-lane point editing, and acknowledged detached vector capture.
 */
package io.github.mundanej.map.vaadin;
