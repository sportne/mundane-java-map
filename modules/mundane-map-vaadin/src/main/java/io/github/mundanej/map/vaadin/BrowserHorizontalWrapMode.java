package io.github.mundanej.map.vaadin;

/** Explicit browser binding policy for horizontal display repetition. */
public enum BrowserHorizontalWrapMode {
    /** Keep the binding in its ordinary local, non-repeating display domain. */
    NONE,
    /** Repeat checked canonical content across the configured horizontal world profile. */
    REPEAT_X
}
