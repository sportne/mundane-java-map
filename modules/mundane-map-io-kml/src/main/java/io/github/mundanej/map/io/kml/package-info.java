/**
 * Secure, bounded, read-only support for the project KML 2.2 geometry profile.
 *
 * <p>The public entry point accepts local files only. KML presentation, network, and dynamic
 * constructs are never resolved.
 */
package io.github.mundanej.map.io.kml;
