/**
 * Bounded MBTiles 1.3 container support using the explicit Xerial adapter.
 *
 * <p>The released profile is read-only PNG/JPEG raster access. The approved G19 target, tracked in
 * the module capability matrix, adds compatible table/view reading, complete metadata and raw
 * media, MVT 2.1, legacy UTFGrid 1.3 interoperability, builder creation, and transactional
 * recognized-schema editing without exposing JDBC, Jackson, ImageIO, or third-party types.
 */
package io.github.mundanej.map.io.mbtiles;
