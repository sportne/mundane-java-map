/**
 * JDK-only implementations of the toolkit-neutral map contracts and algorithms.
 *
 * <p>This package supplies immutable in-memory snapshots and sources, explicit CRS/projection
 * registration, viewport and symbol layout, hit testing and measurement, bounded query accounting,
 * packed spatial indexing, and screen clipping/simplification. It depends only on {@code
 * mundane-map-api}; it exposes neither AWT nor external-library types and performs no automatic
 * implementation discovery.
 */
package io.github.mundanej.map.core;
