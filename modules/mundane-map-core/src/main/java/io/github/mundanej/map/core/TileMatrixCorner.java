package io.github.mundanej.map.core;

/** Vertical corner from which TileMatrixSet row zero is counted. */
public enum TileMatrixCorner {
    /** Row zero starts at the upper edge and row numbers increase downward. */
    TOP_LEFT,
    /** Row zero starts at the lower edge and row numbers increase upward. */
    BOTTOM_LEFT
}
