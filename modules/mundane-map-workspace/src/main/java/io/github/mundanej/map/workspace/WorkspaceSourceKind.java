package io.github.mundanej.map.workspace;

/** Closed workspace source roles used by explicit opener registrations. */
public enum WorkspaceSourceKind {
    /** Toolkit-neutral vector feature source. */
    FEATURE,
    /** Toolkit-neutral raster source. */
    RASTER
}
