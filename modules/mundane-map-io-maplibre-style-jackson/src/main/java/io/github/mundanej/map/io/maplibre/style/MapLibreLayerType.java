package io.github.mundanej.map.io.maplibre.style;

/** Layer types supported by the bounded vector-style profile. */
public enum MapLibreLayerType {
    /** Point circles. */
    CIRCLE,
    /** Linear geometry. */
    LINE,
    /** Polygon interiors and outlines. */
    FILL,
    /** Point icon with optional point label. */
    SYMBOL
}
